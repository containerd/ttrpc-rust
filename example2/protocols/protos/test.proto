syntax = "proto3";

package test;

import "google/protobuf/test_empty.proto";

message TestMessage {
    string Name = 1;
};

// A few TestService comments
service TestService {
    rpc Echo(TestMessage) returns (TestMessage);
    rpc EchoEmpty(TestMessage) returns (google.protobuf.Empty);
};
